# ProjectCRoCController
Controller for Project CRoC
Platform.io repository, including 2 cpp files to be used on esp32-c3:
    - Sender, to be used for the microcontroller embedded into the controller.
    - Receiver, for the use case, will receiver the commands sent from the controller.
